الجزء الأول – Sheet: SYS_DEFINITIONS

تعريف أنواع الجداول (Entities Catalogue)

Column Name	Data Type	Status	Importance	ماذا يفعل + ماذا نفعل في الكود
Def_Key	Text	Mandatory	🔴 حرِج جدًا	المعرّف البرمجي لكل نوع (Entity). يُستخدم كمفتاح رئيسي لكل النظام، ويربط هذا النوع بكل الأعمدة والجداول الأخرى. في الكود: نستخدمه كمفتاح أساسي في TableDefinitionEntity.DefKey، نوعه string، ويجب التعامل معه كقيمة فريدة (Unique) وغير فارغة، واستخدامه كمفتاح في القواميس والـ registry.
Parent_Def_Key	Text	Optional	🟡 مهم لاحقًا	يحدد الوراثة بين الأنواع (inheritance)، بحيث يرث النوع أعمدة نوع آخر. في الكود: نوع string، ويمكن أن يكون null. إذا كانت له قيمة، نستخدمها عند بناء الـ schema لدمج أعمدة الأب مع الإبن (مع منطق override لو نفس Col_Key).
Display_Name	Text	Mandatory	🔴 أساسي	الاسم الظاهر للمستخدم في الواجهة. في الكود: نوع string، نستخدمه في الـ UI (قوائم، شجرة جداول، tooltips)، ممنوع يكون فارغ لو النوع مفعّل.
Super_Category	Text (enum)	Mandatory	🟠 عالية	يصنف النوع تحت مجموعة رئيسية (RESOURCE, CONTEXT, SYSTEM, PRODUCTIVITY) لتسهيل الفلترة والتقارير. في الكود: نخزّنه كـ string لكن نتعامل معه كـ enum منطقي (على الأقل ToUpper() + switch) لاختيار مكان عرض النوع في الـ Ribbon / Panels.
Sort_Order	Integer	Optional	🟡 متوسّط	يحدد ترتيب ظهور الأنواع في القوائم. في الكود: نوع int، مع default = 0 لو لم تُملأ، ونستخدمه في OrderBy(def.SortOrder) عند ترتيب القوائم.
Icon_Name	Text	Optional	🟡 متوسّط	يحدد اسم الأيقونة المستخدمة لتمثيل النوع في الواجهة. في الكود: نوع string، نستخدمه لتحميل OfficeImageId أو أيقونات مخصصة، مع fall-back لأيقونة افتراضية لو كانت فارغة.
Behavior_Tags	Text	Optional	🟠 عالية (سلوكية)	قائمة وسوم (tags) تفعل سلوكيات معيّنة مثل HAS_PRICE, WASTABLE, FORECAST_ENABLED. في الكود: نوع string، نفكّها إلى HashSet<string> (split بـ , أو `
Cache_Policy	Text (enum)	Mandatory	🔴 حرِج	تحدد سياسة التحديث (VOLATILE, NORMAL, STATIC, LOG) لتحديد هل الجدول يستبدل كل مرة أو يدمج… في الكود: نستخدمها في GetUpdateStrategy() لتحويلها إلى enum UpdateStrategy (ReplaceAll / MergeUpsert / Append) والتحكم في منطق الـ ETL.
Access_Level	Text	Mandatory	🟠 عالية	تحدد من يحق له رؤية هذا النوع (FREE, PREMIUM, ADMIN). في الكود: نوع string ونستخدمها في منطق الترخيص (إخفاء/تعطيل بعض الأنواع حسب ترخيص المستخدم).
UI_View_Mode	Text (enum)	Mandatory	🟠 عالية	توجه الواجهة كيف تعرض هذا النوع (GRID, TREE, FORM, CALCULATOR). في الكود: نقرأها كـ string، ونعمل switch لتحديد نوع الـ UI (شيت جدولي، فورم، TreeView…).
ID_Prefix_Rule	Text	Optional	🟡 متوسّط	بادئة تلقائية لأكواد العناصر الجديدة (مثل MAT-). في الكود: تُستخدم عند توليد IDs جديدة يدويًا داخل الأداه، نضيفها قبل الرقم التسلسلي.
Color_Hex	Text	Optional	🟡 تجميلي/تحليلي	لون مخصص للنوع في التقارير والـ charts. في الكود: نوع string، يمكن تحويله إلى Color عند العرض، مع fall-back للون افتراضي لو القيمة غير صالحة.
Is_Active	Boolean	Mandatory	🔴 حرِج	يحدد إن كان النوع مفعّل في النظام. في الكود: نوع bool، إذا كان false يجب إخفاء النوع بالكامل من الواجهة وعدم تحميل بياناته في القوائم.
الجزء الثاني – Sheet: SYS_SCHEMA_RULES

تعريف أعمدة كل نوع (Column Rules)

Column Name	Data Type	Status	Importance	ماذا يفعل + ماذا نفعل في الكود
Def_Key	Text	Mandatory	🔴 حرِج	يربط العمود بنوع معيّن من SYS_DEFINITIONS. في الكود: نستخدمه لتجميع الأعمدة لكل جدول عند بناء TableSchema.Columns.
Col_Key	Text	Mandatory	🔴 حرِج	اسم العمود في SQLite، بدون مسافات. في الكود: نستخدمه كاسم العمود في SQL ([Col_Key]) وفي الـ models (Mapping). ممنوع يكون فارغ أو مكرر داخل نفس الـ Def_Key.
Display_Name	Text	Mandatory	🟠 عالية	عنوان العمود الظاهر للمستخدم في رأس الجدول. في الكود: نستخدمه كـ Header في Excel/ UI، مع fall-back لـ Col_Key لو فاضي.
Data_Type	Text (enum)	Mandatory	🔴 حرِج	نوع البيانات المنطقي (Text, Real, Integer, Date, Boolean). في الكود: نعمل mapping لـ enum SqlDataType (GetSqlType) لتوليد نوع SQLite الصحيح (TEXT/REAL/INTEGER) ونستخدمه في التحويل عند القراءة/الكتابة.
Sort_Order	Integer	Optional	🟡 متوسّط	ترتيب العمود أفقيًا. في الكود: نستخدمه في OrderBy(c => c.SortOrder) عند عرض الأعمدة في الـ UI أو بناء الجداول.
Is_Primary	Boolean	Mandatory	🔴 حرِج	يدل أن هذا العمود هو المفتاح الأساسي للجدول. في الكود: نستخدمه في DDL (PRIMARY KEY)، ونمنع إدخال قيم مكررة (منطق upsert يعتمد عليه).
Is_Required	Boolean	Mandatory	🔴 حرِج	يحدد إن كان العمود لابد أن يحتوي على قيمة. في الكود: نستخدمه في DDL (NOT NULL للحقول غير الـ PK) وفي validation قبل insert (نرفض الصفوف الناقصة أو نكمل بـ Default_Value).
Default_Value	Text	Optional	🟠 عالية	القيمة الافتراضية لو الخلية فاضية. في الكود: عند الـ ETL، لو القيمة null/empty نحقن DefaultValue بدلها، مع تحويل النوع حسب Data_Type (رقم/تاريخ…).
Is_Global_Append	Boolean	Mandatory	🟡 متوسّط	يحدد هل هذا العمود يدخل في عمليات التجميع عبر الجداول (Global Append / Union). في الكود: عند لعبة UNION ALL نختار فقط الأعمدة التي لها Is_Global_Append = TRUE.
License_Tier	Text	Mandatory	🟡 متوسّط	مستوى الترخيص للعمود نفسه (FREE/PREMIUM/ADMIN). في الكود: يمكن استخدامه لإخفاء أعمدة معينة حسب نوع اشتراك المستخدم.
Is_Visible	Boolean	Mandatory	🟠 عالية	يحدد إن كان العمود يظهر في الواجهة. في الكود: نوع bool، نستخدمه عند تجهيز Grid/Sheet لتحديد أي الأعمدة تُعرض وأيها تُخفى.
Is_Editable	Boolean	Mandatory	🟠 عالية	يحدد إن كان المستخدم يمكنه تعديل العمود يدويًا. في الكود: نضبط خصائص الـ UI (Locked/Protected) حسب هذه القيمة.
UI_Width	Integer	Optional	🟡 تجميلي/تحسيني	عرض العمود بالبكسل. في الكود: نستخدمه عند رسم الأعمدة في Excel/Grids لتحديد Column.Width إذا كانت > 0.
Format_String	Text	Optional	🟡 متوسّط	صيغة العرض (مثل N2, dd/MM/yyyy). في الكود: نستخدمها لتطبيق تنسيق الأرقام/التواريخ في Excel أو في عرض النصوص.
Validation_Rule	Text	Optional	🟡 متقدّم	قواعد للتأكد من صحة المدخلات (MIN, MAX, LIST…). في الكود: يمكن ترجمتها إلى منطق Validation (مثلاً parsing إلى tokens) لتطبيق قواعد على إدخال المستخدم أو للتحذير.
Aggregation_Type	Text	Optional	🟡 تحليلي	يحدد طريقة تجميع العمود في التقارير (SUM, AVG, MAX, MIN, NONE). في الكود: نستخدمه عند توليد تقارير Pivot/ملخصات لتحديد نوع الدالة لكل عمود.
Description	Text	Optional	🟡 توثيقي	وصف حر لما يمثله العمود. في الكود: نستخدمه في tooltips أو مستندات التكوين، لا تدخل في المنطق الحسابي.
Ref_Def_Key	Text	Optional	🟠 قوية للربط	يربط العمود بـ Entity آخر (Foreign Key منطقي). في الكود: يمكن استخدامه عند بناء علاقات/Breakdown (مثلاً Item → Category) وعند توليد شاشات اختيار (Lookup) من جدول آخر.
Semantic_Role	Text	Optional	🟠 مهمة للـ BI	تصنيف semantic للدور (KEY, DIMENSION, MEASURE, ATTRIBUTE, FLAG). في الكود: نستخدمها داخل محرك الـ breakdown لاختيار الأعمدة المستخدمة في المحور (Dimensions) أو في الحساب (Measures) أو المفاتيح.
الجزء الثالث – Sheet: MASTER_DIRECTORY

دليل مصادر البيانات (Data Sources Catalogue)

Column Name	Data Type	Status	Importance	ماذا يفعل + ماذا نفعل في الكود
Table_Key	Text	Mandatory	🔴 حرِج	اسم الجدول الفيزيائي في SQLite. في الكود: نستخدمه كاسم الجدول في SQL (CREATE/INSERT/SELECT) وكـ مفتاح في _Sys_Versions، لازم يكون فريد.
Def_Key	Text	Mandatory	🔴 حرِج	يربط هذا الجدول بتعريف منطقي في SYS_DEFINITIONS. في الكود: نستخدمه لاختيار TableSchema الصحيح عند المزامنة.
Display_Name	Text	Mandatory	🟠 عالية	الاسم الظاهر لهذا الجدول في الواجهة (شجرة الجداول، الـ dropdown…). في الكود: يستخدم لعرض القائمة للمستخدم بدل Table_Key.
Source_Url	Text (URL)	Mandatory	🔴 حرِج	رابط ملف البيانات (TSV أو Google Sheet Export). في الكود: نستخدمه مباشرة في HttpClientService لتنزيل المحتوى، يجب أن يكون URL صالح.
Map_Group_Key	Text	Optional	🟡 متقدّم	يربط عدة جداول بمجموعة ترجمة واحدة في SYS_DATA_MAP. في الكود: يسمح بإعادة استخدام نفس قواعد الـ mapping لسيناريوهات متعددة (مثلاً نفس ملف بصيغة مختلفة).
Region_Code	Text	Optional	🟡 جغرافي	كود المنطقة (EG, SA, UAE…). في الكود: يُستخدم للفلترة (اختيار جداول حسب المنطقة) أو في التقارير.
Provider_Name	Text	Optional	🟡 توثيقي	اسم مزوّد البيانات (Source). في الكود: للعرض فقط/التقارير.
Version	Text	Mandatory	🔴 حرِج	رقم إصدار الجدول. تغييره يجبر الـ Sync يعيد تحميل الجدول. في الكود: نحتفظ به في _Sys_Versions ونقارن مع الموجود محليًا، لو اختلف → نعمل إعادة مزامنة.
Access_Tier	Text	Mandatory	🟡 متوسّط	مستوى الوصول للجدول (FREE, PREMIUM…). في الكود: يُستخدم لإخفاء بعض الجداول حسب اشتراك المستخدم.
Update_Frequency	Text	Optional	🟡 تحكّم	يحدد تكرار التحديث لهذا الجدول تحديدًا (يُغلب Policy العامة لو موجود). في الكود: يمكن استخدامه لتحديد متى نعيد المزامنة تلقائيًا أو في المستقبل.
Is_Active	Boolean	Mandatory	🔴 حرِج	يحدد إن كان الجدول مشاركًا في المزامنة ولا لأ. في الكود: لو false نتجاهله تمامًا في Sync، حتى لو الـ Def_Key نفسه مفعّل.
الجزء الرابع – Sheet: SYS_DATA_MAP

خريطة تحويل البيانات من Raw → Schema قياسي

Column Name	Data Type	Status	Importance	ماذا يفعل + ماذا نفعل في الكود
Table_Key	Text	Mandatory	🔴 حرِج	يحدد أي جدول (من MASTER_DIRECTORY) تنطبق عليه هذه القاعدة. في الكود: نستخدمه لتجميع قواعد الـ mapping لكل جدول عند قراءة ملفه. (أو لاحقًا Map_Group_Key).
Map_Group_Key	Text	Optional	🟡 متقدّم	مجموعة قواعد مشتركة يمكن ربطها بأكثر من جدول. في الكود: تسمح بإعادة استخدام نفس الـ mapping لجداول متعددة إن احتجنا.
Raw_Header	Text	Conditional	🔴 مهم	اسم العمود في الملف الأصلي. في الكود: لو موجود، نبحث في الـ TSV headers عن نفس الاسم (case-insensitive)، ونأخذ القيمة منه. إذا فارغ يعتمد على وجود Static_Value.
Target_Col_Key	Text	Mandatory	🔴 حرِج	يحدد أي عمود قياسي في SYS_SCHEMA_RULES سيتم ملؤه. في الكود: نستخدمه للربط مع SchemaRuleEntity.ColKey وتحديد مكان وضع القيمة في سطر الـ Dictionary قبل الإدخال.
Static_Value	Text	Conditional	🟠 عالية	قيمة ثابتة تُحقن في كل الصفوف (مثلاً Region = EG) إذا لم يوجد عمود مكافئ في الملف. في الكود: لو Raw_Header فارغ وStatic_Value موجودة، نكتب هذه القيمة مباشرة بدون قراءة من الملف.
Transform_Rule	Text	Optional	🟡 متقدّم	دوال بسيطة لتنظيف/تحويل البيانات أثناء التحميل (TRIM, UPPER, TO_NUMBER, …). في الكود: نطبّق سلسلة من التحويلات على القيمة المقروءة قبل حفظها، يمكن لاحقًا توسيعها إلى mini-DSL.
