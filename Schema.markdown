Sheet 1 – SYS_DEFINITIONS
تعريف أنواع الجداول (Entities Catalogue)
| Column Name      | Data Type   | Status        | Importance        | الوصف + ما يجب مراعاته في الكود                                                                                                                                                                                     |
| ---------------- | ----------- | ------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Def_Key`        | Text        | **Mandatory** | 🔴 حرِج جدًا      | المعرّف البرمجي لكل نوع (Entity). يستخدم كمفتاح أساسي في كل النظام (ربط الأعمدة، الجداول، الـ registry). في الكود: `string` غير فارغ، فريد، يُستخدم كمفتاح في `SchemaRegistry` وكمفتاح رئيسي للـ TableSchema.       |
| `Parent_Def_Key` | Text        | Optional      | 🟡 مهم لاحقًا     | يحدد الوراثة بين الأنواع. في الكود: `string` يمكن أن يكون `null`. لو له قيمة، عند بناء الـ schema ندمج أعمدة الأب مع الابن (مع منطق override لو نفس `Col_Key`).                                                     |
| `Display_Name`   | Text        | **Mandatory** | 🔴 أساسي          | الاسم الظاهر للمستخدم في واجهة البرنامج. في الكود: يُستخدم في القوائم، الشجرات، الـ tooltips، ويُفضل التحقق ألا يكون فارغًا لو `Is_Active = true`.                                                                  |
| `Super_Category` | Text (enum) | **Mandatory** | 🟠 عالية          | تصنيف أعلى للتنظيم (`RESOURCE`, `CONTEXT`, `SYSTEM`, `PRODUCTIVITY`). في الكود: نخزنها كـ `string` لكن نتعامل معها كـ enum منطقي (switch) لتحديد أماكن العرض والفلاتر.                                              |
| `Sort_Order`     | Integer     | Optional      | 🟡 متوسّط         | رقم لتحديد ترتيب ظهور النوع في القوائم. في الكود: `int` مع default = 0، نستخدمه في `OrderBy(def.SortOrder)`.                                                                                                        |
| `Icon_Name`      | Text        | Optional      | 🟡 متوسّط         | اسم أيقونة الـ UI. في الكود: `string` نستخدمه لتعيين `OfficeImageId` أو أيقونة مخصّصة مع fall-back لأيقونة افتراضية لو كانت فارغة.                                                                                  |
| `Behavior_Tags`  | Text        | Optional      | 🟠 عالية (سلوكية) | قائمة وسوم لتفعيل سلوكيات خاصة (مثل: `HAS_PRICE`, `WASTABLE`, `FORECAST_ENABLED`). في الكود: نجزئها إلى `string[]` أو `HashSet<string>` ونستخدمها لتفعيل ميزات أو أعمدة أو حسابات معينة.                            |
| `Cache_Policy`   | Text (enum) | **Mandatory** | 🔴 حرِج           | سياسة التحديث (`VOLATILE`, `NORMAL`, `STATIC`, `LOG`). في الكود: نستخدمها في `TableDefinitionEntity.GetUpdateStrategy()` لتحويلها إلى `UpdateStrategy` (ReplaceAll / MergeUpsert / Append) للتحكم في منطق الـ Sync. |
| `Access_Level`   | Text        | **Mandatory** | 🟠 عالية          | يحدد من يحق له رؤية هذا النوع (`FREE`, `PREMIUM`, `ADMIN`). في الكود: نستخدمه في طبقة الترخيص لإخفاء/إظهار بعض الأنواع حسب نوع المستخدم.                                                                            |
| `UI_View_Mode`   | Text (enum) | **Mandatory** | 🟠 عالية          | يحدد طريقة عرض البيانات في الواجهة (`GRID`, `TREE`, `FORM`, `CALCULATOR`). في الكود: switch لتحديد نوع الشاشة/الشيت/الفورم المستخدم لهذا النوع.                                                                     |
| `ID_Prefix_Rule` | Text        | Optional      | 🟡 متوسّط         | بادئة للكود عند إضافة عنصر جديد يدويًا (مثل `MAT-`). في الكود: تُستخدم عند توليد IDs جديدة داخل النظام.                                                                                                             |
| `Color_Hex`      | Text        | Optional      | 🟡 تجميلي         | لون مخصص للنوع في التقارير (Hex). في الكود: يمكن تحويله لـ `Color` مع fall-back للون افتراضي لو القيمة غير صحيحة.                                                                                                   |
| `Is_Active`      | Boolean     | **Mandatory** | 🔴 حرِج           | تشغيل/إيقاف النوع بالكامل. في الكود: لو `false` لا نعرضه في الواجهة ولا نستخدمه في الـ Sync أو القوائم.                                                                                                             |

Sheet 2 – SYS_SCHEMA_RULES
تعريف أعمدة كل نوع (Column Rules)
| Column Name        | Data Type   | Status        | Importance     | الوصف + ما يجب مراعاته في الكود                                                                                                                                                                   |
| ------------------ | ----------- | ------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Def_Key`          | Text        | **Mandatory** | 🔴 حرِج        | يربط العمود بنوع معيّن من `SYS_DEFINITIONS`. في الكود: نستخدمه لتجميع الأعمدة لكل `TableSchema`.                                                                                                  |
| `Col_Key`          | Text        | **Mandatory** | 🔴 حرِج        | الاسم البرمجي للعمود في SQLite (بدون مسافات). في الكود: يُستخدم في SQL (`[Col_Key]`) وفي الـ mapping، يجب أن يكون فريدًا داخل نفس `Def_Key`.                                                      |
| `Display_Name`     | Text        | **Mandatory** | 🟠 عالية       | عنوان العمود الظاهر للمستخدم. في الكود: Header في Excel/Grids، مع fall-back إلى `Col_Key` لو فارغ.                                                                                                |
| `Data_Type`        | Text (enum) | **Mandatory** | 🔴 حرِج        | نوع البيانات المنطقي (`Text`, `Real`, `Integer`, `Date`, `Boolean`). في الكود: نستخدمه لتحويل القيمة إلى enum `SqlDataType` وتوليد نوع SQLite (`TEXT/REAL/INTEGER`) والتحويل عند القراءة/الكتابة. |
| `Sort_Order`       | Integer     | Optional      | 🟡 متوسّط      | ترتيب العمود في الواجهة. في الكود: `OrderBy(c.SortOrder)` عند توليد الجداول/الشيت.                                                                                                                |
| `Is_Primary`       | Boolean     | **Mandatory** | 🔴 حرِج        | يحدد أن هذا العمود هو المفتاح الأساسي. في الكود: DDL (`PRIMARY KEY`)، منطق upsert، منع تكرار القيم.                                                                                               |
| `Is_Required`      | Boolean     | **Mandatory** | 🔴 حرِج        | يحدد أن العمود لا يقبل قيمة فارغة منطقيًا. في الكود: validation قبل الـ Insert/Update (إما نرفض الصف أو نستخدم `Default_Value`).                                                                  |
| `Default_Value`    | Text        | Optional      | 🟠 عالية       | قيمة افتراضية تملأ الخلايا الفارغة. في الكود: لو القيمة `null/empty` نستخدم `Default_Value` مع تحويل حسب `Data_Type`.                                                                             |
| `Is_Global_Append` | Boolean     | **Mandatory** | 🟡 متوسّط      | هل يدخل هذا العمود في الـ UNION عبر جداول متعددة. في الكود: نستخدمه عند تكوين Queries موحدة (Global Append).                                                                                      |
| `License_Tier`     | Text        | **Mandatory** | 🟡 متوسّط      | مستوى ترخيص العمود (`FREE`, `PREMIUM`, `ADMIN`). في الكود: يمكن استخدامه لإخفاء بعض الأعمدة في النسخ المجانية.                                                                                    |
| `Is_Visible`       | Boolean     | **Mandatory** | 🟠 عالية       | يحدد ظهور العمود في الواجهة. في الكود: لو `false` لا نعرضه في Grid/Sheet لكن يبقى موجودًا في DB.                                                                                                  |
| `Is_Editable`      | Boolean     | **Mandatory** | 🟠 عالية       | يحدد إن كان المستخدم يستطيع تعديل العمود يدويًا. في الكود: نحول العمود إلى Locked/ReadOnly لو `false`.                                                                                            |
| `UI_Width`         | Integer     | Optional      | 🟡 تجميلي      | عرض العمود بالبكسل. في الكود: نطبقه على أعمدة Excel/Grids إذا القيمة > 0.                                                                                                                         |
| `Format_String`    | Text        | Optional      | 🟡 متوسّط      | تنسيق العرض (`N2`, `dd/MM/yyyy`, …). في الكود: نستخدمه لتطبيق NumberFormat/DateFormat في Excel أو العرض.                                                                                          |
| `Validation_Rule`  | Text        | Optional      | 🟡 متقدّم      | قواعد التحقق من صحة المدخلات (مثل: `MIN:0`, `LIST:m3,ton,kg`). في الكود: يمكن تحليلها وتطبيقها قبل حفظ البيانات أو عند إدخال المستخدم.                                                            |
| `Aggregation_Type` | Text (enum) | Optional      | 🟡 تحليلي      | نوع التجميع في التقارير (`SUM`, `AVG`, `MAX`, `MIN`, `NONE`). في الكود: نستخدمها عند توليد Pivot/Reports.                                                                                         |
| `Description`      | Text        | Optional      | 🟡 توثيقي      | وصف واضح للعمود. في الكود: للـ tooltips أو التوثيق الداخلي.                                                                                                                                       |
| `Ref_Def_Key`      | Text        | Optional      | 🟠 قوية للربط  | يربط العمود بـ Entity آخر (علاقة منطقية/Foreign Key). في الكود: يساعد في بناء علاقات وربط الجداول (مثلاً Item → Category) وفي شاشات الـ Lookup.                                                   |
| `Semantic_Role`    | Text (enum) | Optional      | 🟠 مهمة للـ BI | الدور التحليلي: `KEY`, `DIMENSION`, `MEASURE`, `ATTRIBUTE`, `FLAG`. في الكود: يستخدم في محرك الـ breakdown لاختيار ما يُستخدم كمفتاح، وما هو بعد تحليلي، وما هو قياس رقمي.                        |

Sheet 3 – MASTER_DIRECTORY
دليل مصادر البيانات (Data Sources Catalogue)
| Column Name        | Data Type  | Status        | Importance | الوصف + ما يجب مراعاته في الكود                                                                                                              |
| ------------------ | ---------- | ------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `Table_Key`        | Text       | **Mandatory** | 🔴 حرِج    | اسم الجدول الفيزيائي في SQLite. في الكود: نستخدمه حرفيًا في أوامر SQL وفي `_Sys_Versions`، يجب أن يكون فريدًا.                               |
| `Def_Key`          | Text       | **Mandatory** | 🔴 حرِج    | يربط هذا الجدول بالتعريف المنطقي في `SYS_DEFINITIONS`. في الكود: نستخدمه لاختيار `TableSchema` الصحيح أثناء الـ Sync.                        |
| `Display_Name`     | Text       | **Mandatory** | 🟠 عالية   | الاسم الذي يظهر للمستخدم عند اختيار مصدر البيانات. في الكود: يُستخدم لعنونة الجداول في القوائم والشيتات.                                     |
| `Source_Url`       | Text (URL) | **Mandatory** | 🔴 حرِج    | رابط ملف البيانات (TSV/Google Sheet Export). في الكود: نستخدمه مع `HttpClientService` لتنزيل البيانات، يجب التحقق من صحته.                   |
| `Map_Group_Key`    | Text       | Optional      | 🟡 متقدّم  | يربط عدة جداول بخريطة ترجمة واحدة في `SYS_DATA_MAP`. في الكود: يسمح بإعادة استخدام نفس قواعد الـ mapping لأكثر من جدول.                      |
| `Region_Code`      | Text       | Optional      | 🟡 جغرافي  | كود المنطقة (`EG`, `SA`, `UAE`, `GLOBAL`). في الكود: يستخدم للفلترة أو التقارير حسب المنطقة.                                                 |
| `Provider_Name`    | Text       | Optional      | 🟡 توثيقي  | اسم مزوّد البيانات أو المصدر. في الكود: للعرض فقط.                                                                                           |
| `Version`          | Text       | **Mandatory** | 🔴 حرِج    | رقم إصدار الجدول. تغييره يعني أن المحتوى تغيّر. في الكود: نستخدمه للمقارنة مع النسخة المحلية في `_Sys_Versions`، لو مختلف → نعيد المزامنة.   |
| `Access_Tier`      | Text       | **Mandatory** | 🟡 متوسّط  | مستوى الوصول (`FREE`, `PREMIUM`). في الكود: يمكن استخدامه لإخفاء جداول في النسخة المجانية.                                                   |
| `Update_Frequency` | Text       | Optional      | 🟡 تحكّم   | تواتر التحديث الخاص بهذا الجدول تحديدًا (يُغلب على الـ Policy العامة لو تم استخدامه). في الكود: يمكن استخدامه لاحقًا في منطق جدولة المزامنة. |
| `Is_Active`        | Boolean    | **Mandatory** | 🔴 حرِج    | تشغيل/إيقاف جدول معين. في الكود: لو `false` يتم تجاهل هذا الصف بالكامل من المزامنة والقوائم.                                                 |

Sheet 4 – SYS_DATA_MAP
ربط البيانات الخام بالسكيمة القياسية (Mapping Rules)
| Column Name      | Data Type | Status        | Importance | الوصف + ما يجب مراعاته في الكود                                                                                                                                    |
| ---------------- | --------- | ------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Table_Key`      | Text      | **Mandatory** | 🔴 حرِج    | يحدد أي جدول مستهدف من `MASTER_DIRECTORY`. في الكود: نستخدمه لتجميع قواعد الـ mapping لكل جدول عند قراءة ملفه.                                                     |
| `Map_Group_Key`  | Text      | Optional      | 🟡 متقدّم  | ربط قواعد مشتركة بمجموعة جداول. في الكود: يسمح بإعادة استخدام نفس قواعد الـ mapping في أكثر من جدول.                                                               |
| `Raw_Header`     | Text      | Conditional   | 🔴 مهم     | اسم العمود كما يظهر في الملف الخام. في الكود: لو موجود نبحث عن هذا الاسم في عناوين الـ TSV (case-insensitive) ونقرأ القيمة منه.                                    |
| `Target_Col_Key` | Text      | **Mandatory** | 🔴 حرِج    | العمود القياسي الذي سيتم ملؤه (يجب أن يطابق `Col_Key` في `SYS_SCHEMA_RULES`). في الكود: نستخدمه للربط بين البيانات الخام وعمود السكيمة القياسي داخل `TableSchema`. |
| `Static_Value`   | Text      | Conditional   | 🟠 عالية   | قيمة ثابتة تُحقن في كل الصفوف عندما لا يوجد عمود مكافئ في الملف. في الكود: لو `Raw_Header` فارغ و`Static_Value` موجودة → نستخدمها مباشرة بدل القراءة من الملف.     |
| `Transform_Rule` | Text      | Optional      | 🟡 متقدّم  | دوال تحويل بسيطة (`TRIM`, `UPPER`, `TO_NUMBER`, …). في الكود: نطبقها على القيمة المقروءة قبل تخزينها (يمكن تحويلها إلى mini-DSL أو سلسلة If/Switch).               |

