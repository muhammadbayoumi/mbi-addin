هذا الجدول الاسترشادي هو "الخريطة المركزية" لنظامك. هو يوضح لأي شخص (سواء كان مهندس بيانات أو مبرمج) كيف يقوم بتعريف جدول جديد في النظام عبر جدول **`SYS_DEFINITIONS`**.

بناءً على الكود الأخير لـ **`TableDefinitionEntity`**، إليك الدليل الشامل:

### 📘 الدليل المرجعي لجدول تعريف الجداول (`SYS_DEFINITIONS`)

| اسم العمود في قاعدة البيانات | الوصف الوظيفي (Description) | الدور المنطقي (Logical Role) | إلزامي؟ | القيم المسموح بها / أمثلة |
| :--- | :--- | :--- | :--- | :--- |
| **`ENTITY_KEY`** | المعرف الفريد للجدول في النظام | Primary Key | **نعم** | نص إنجليزي كبير (مثال: `T_MATERIALS`) |
| **`DISPLAY_NAME`** | الاسم الذي يظهر للمستخدم في الواجهة | Label | **نعم** | أي نص (مثال: جدول الخامات الأساسي) |
| **`PARENT_KEY`** | كود الجدول الأب لوراثة الخصائص منه | Inheritance | لا | `ENTITY_KEY` لجدول آخر موجود فعلياً |
| **`CATEGORY`** | تصنيف الجدول ضمن هيكلية النظام | Data Layer | **نعم** | `METADATA`, `LOOKUP`, `TEMPLATE`, `OPERATIONAL` |
| **`ENTITY_TYPE`** | طبيعة البيانات المخزنة (مالية، فنية..) | Processing Logic | نعم | `REF`, `COST`, `PERF`, `COMP` |
| **`BUSINESS_DOMAIN`** | المجال التخصصي للبيانات | Filter / Domain | نعم | `MATERIAL`, `LABOR`, `EQUIPMENT`, `GENERAL` |
| **`IS_SYSTEM_TABLE`** | هل الجدول جزء من ملفات النظام المحمية؟ | Security Flag | نعم | `TRUE` (محمي), `FALSE` (جدول مستخدم) |
| **`PK_TYPE`** | طريقة توليد المفتاح الأساسي للسطور | Storage Logic | لا | `AutoGUID`, `Composite`, `Manual` |
| **`ALLOW_EDIT`** | السماح للمستخدم بتعديل البيانات | Permission | نعم | `TRUE`, `FALSE` |
| **`ALLOW_EXPORT`** | السماح بتصدير البيانات خارج النظام | Permission | نعم | `TRUE`, `FALSE` |
| **`LICENSE_TIER`** | مستوى الترخيص المطلوب للوصول للجدول | Security Tier | نعم | `Free`, `Standard`, `Gold`, `Enterprise`, `Admin` |
| **`STORAGE_STRATEGY`** | طريقة حفظ وتحديث البيانات | Sync Strategy | نعم | `MergeUpsert`, `ReplaceAll`, `Append` |
| **`VIEW_MODE`** | شكل عرض الجدول في برنامج إكسل | UI Strategy | نعم | `Table`, `Breakdown`, `Form`, `Pivot` |
| **`UI_GROUP`** | اسم التبويب في القائمة العلوية | UI Grouping | لا | مثال: `Engineering`, `Finance`, `Setup` |
| **`UI_ORDER`** | ترتيب ظهور الجدول في القائمة | UI Ordering | لا | أرقام صحيحة (0, 1, 2, 3...) |
| **`ICON_KEY`** | كود الأيقونة المرتبطة بالجدول | UI Styling | لا | مثال: `Brick`, `Users`, `Calculator` |
| **`IS_ACTIVE`** | حالة الجدول (مفعل أو معطل) | Logic Flag | نعم | `TRUE`, `FALSE` |
| **`UX_CONFIG`** | حقيبة التنسيقات الجمالية الخاصة بإكسل | **JSON Bag** | لا | `{ "Color": "#Hex", "Zoom": 90, "Freeze": "A2" }` |

---

### 💡 ملاحظات هامة لمستخدم النظام:

1.  **قوة الـ `ENTITY_KEY`:** لا تقم بتغيير هذا المفتاح بمجرد إنشاء الجدول، لأنه يربط كافة الأعمدة والعلاقات في الجداول الأخرى.
2.  **استراتيجية التوريث (`PARENT_KEY`):** إذا قمت بإنشاء جدول "خامات الأسمنت" وجعلت الأب هو "قالب الخامات العام"، فإنك لست مضطراً لتعبئة حقول مثل `VIEW_MODE` أو `LICENSE_TIER` لأن النظام سيسحبها تلقائياً من الأب.
3.  **الـ JSON Bag (`UX_CONFIG`):** تذكر دائماً؛ أي شيء تضعه هنا هو "للعرض فقط". لا تضع هنا أي معلومة يحتاجها النظام لاتخاذ قرار محاسبي أو تقني.
4.  **تصنيف `CATEGORY`:**
    *   استخدم `METADATA` فقط للجداول التي تدير النظام.
    *   استخدم `LOOKUP` لجداول الثوابت (وحدات، عملات).
    *   استخدم `OPERATIONAL` لجداول البيانات الفعلية للمشاريع.

هذا الجدول يضمن أن تكون قاعدة البيانات الخاصة بك **نظيفة، مفهومة، وقابلة للتطوير** دون الحاجة للرجوع للمبرمج في كل مرة تريد فيها إضافة جدول جديد.

بناءً على طلبك، قمت بتصميم جدول تقني يوضح "خلف الكواليس" لكل عمود. هذا الجدول يشرح أين يذهب كل حقل في الكود، وكيف تتعامل معه في قاعدة البيانات، وما هي تكلفة التعديل (إضافة قيم جديدة).

### 1. الجدول التقني لإدارة وتطبيق الـ Metadata

هذا الجدول يربط بين **الأعمدة** و **المحركات (Engines)** التي تستخدمها:

| اسم العمود | نوع البيانات (DB Type) | مكان التطبيق (Applied In) | كيف تضاف قيمة جديدة؟ (Extensibility) |
| :--- | :--- | :--- | :--- |
| **ENTITY_KEY** | `NVARCHAR(50)` | **كل المحركات:** هو المفتاح الذي يربط كل شيء. | **سهل جداً:** مجرد إضافة سطر جديد في جدول التعريفات. |
| **CATEGORY** | `INT / NVARCHAR` | **UI Engine:** يحدد مكان ظهور الجدول في القائمة. | **متوسط:** إضافة قيمة في `TableCategory` Enum وتعديل واجهة القائمة. |
| **ENTITY_TYPE** | `INT / NVARCHAR` | **Calculation Engine:** يحدد المعادلات الرياضية المطبقة. | **صعب:** إضافة قيمة في الـ Enum وتعديل محرك الحسابات ليفهم النوع الجديد. |
| **BUSINESS_DOMAIN** | `INT / NVARCHAR` | **Filter Engine:** يحدد التصنيفات الهندسية. | **سهل:** إضافة قيمة في الـ Enum (تعديل كود بسيط). |
| **STORAGE_STRATEGY** | `INT` | **Sync Engine (ETL):** يحدد كيف يتم المسح والحفظ. | **صعب:** إضافة قيمة في الـ Enum تتطلب كتابة "كود معالجة" جديد في محرك الـ Sync. |
| **VIEW_MODE** | `INT` | **UI Engine:** يحدد شكل الشيت (Table vs Tree). | **صعب:** إضافة قيمة جديدة تتطلب بناء "متحكم واجهة" (View Controller) جديد. |
| **PK_TYPE** | `NVARCHAR(20)` | **Storage Engine:** يحدد طريقة توليد المفتاح. | **متوسط:** إضافة كلمة جديدة تتطلب تعديل شرط `Switch/Case` في كود الحفظ. |
| **LICENSE_TIER** | `INT` | **Security Engine:** يقارن رتبة المستخدم برتبة الجدول. | **سهل:** إضافة رتبة جديدة في الـ Enum فقط (المقارنة رقمية). |
| **ALLOW_EDIT** | `BIT / BOOLEAN` | **Authorization Engine:** يقفل أو يفتح الخلايا. | **ثابت:** لا يحتاج لإضافة قيم (نعم أو لا). |
| **UX_CONFIG** | `NVARCHAR(MAX)` | **Excel Formatting Engine:** يطبق الألوان والزووم. | **سهل جداً:** أضف أي مفتاح جديد داخل الـ JSON وسيتعامل معه الكود ديناميكياً. |

---

### 2. دليل "ماذا أفعل لو أردت الإضافة؟"

لكي تعرف حجم الجهد المطلوب عند رغبتك في تطوير النظام مستقبلاً، اتبع هذا الدليل:

#### أ. إضافة "جدول جديد" (مثل جدول معدات)
*   **الإجراء:** أضف سطر جديد في `SYS_DEFINITIONS`.
*   **الجهد:** 1% (لا يحتاج برمجة).
*   **المتطلبات:**ENTITY_KEY جديد، وربطه بـ EntityType موجود (مثل COST).

#### ب. إضافة "نوع بيانات/حسابات جديد" (مثل إضافة نوع ENTITY_TYPE = "TIME_SHEET")
*   **الإجراء:**
    1.  تعديل ملف `CommonEnums.cs` لإضافة النوع الجديد.
    2.  تعديل كود `CostEngine.cs` ليشرح للنظام: "إذا كان النوع TIME_SHEET، قم بجمع الساعات كذا وكذا".
*   **الجهد:** 50% (يحتاج برمجة وتعديل في المحرك الحسابي).

#### ج. إضافة "تنسيق جمالي جديد" (مثل إضافة "حجم الخط" في الإكسل)
*   **الإجراء:**
    1.  أضف خاصية `"FontSize": 12` داخل حقيبة الـ `UX_CONFIG` في الإكسل.
    2.  تعديل كود الـ `ExcelFormatter` ليقرأ هذا المفتاح الجديد.
*   **الجهد:** 10% (تعديل بسيط في واجهة المستخدم فقط).

---

### 3. نصيحة تقنية (تجنب تعديل الـ Enums بكثرة)

بما أنك لاحظت أن `ENTITY_TYPE` يتطلب تعديلات كثيرة، إليك القاعدة الذهبية:
*   اجعل الـ **Enums** للأشياء التي تمثل **"سلوكاً برمجياً ثابتاً"** (مثل: كيف يتم الحساب؟ كيف يتم العرض؟).
*   اجعل الـ **Metadata (الأعمدة والـ JSON)** للأشياء التي تمثل **"بيانات وصفية"** (مثل: اسم الجدول، لونه، المجموعة التي ينتمي إليها).

**بهذا التخطيط، يمكنك إضافة 100 جدول جديد لنظامك دون كتابة سطر كود واحد، طالما أنها تتبع نفس "أنواع الحسابات" (ENTITY_TYPES) الموجودة مسبقاً.**
ليكون جاهزاً للربط مع هذه الجداول؟
